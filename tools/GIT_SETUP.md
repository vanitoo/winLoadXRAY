# Git настройки для winLoadXRAY

## Файлы конфигурации Git

Созданы файлы для оптимальной работы с Git в проекте winLoadXRAY:

### 📁 `.gitignore`
Исключает ненужные файлы из репозитория:
- Виртуальные окружения (`.venv/`, `venv/`)
- Кэшированные файлы Python (`__pycache__/`)
- Внешние компоненты (`xray/`, `tun2proxy/`)
- Пользовательские данные (`configs/`)
- Системные файлы (`.DS_Store`, `Thumbs.db`)

### 📁 `.gitattributes`
Настраивает поведение Git:
- **Переводы строк**: Windows файлы (`*.bat`, `*.ps1`) используют CRLF
- **Языки программирования**: Корректное определение Python, Markdown, JSON
- **Бинарные файлы**: Правильная обработка изображений и исполняемых файлов
- **Статистика языков**: Исключение документации и сгенерированных файлов

## Быстрый старт

### 1. Инициализация репозитория
```bash
git init
git add .
git commit -m "Initial commit: winLoadXRAY with modern UI"
```

### 2. Подключение к удаленному репозиторию
```bash
git remote add origin https://github.com/username/winLoadXRAY.git
git branch -M main
git push -u origin main
```

### 3. Проверка игнорируемых файлов
```bash
# Посмотреть статус с игнорируемыми файлами
git status --ignored

# Проверить конкретный файл
git check-ignore xray/xray.exe
```

## Структура файлов в репозитории

### ✅ Включенные файлы
```
📁 Проект
├── 📄 winLoadXRAY.py              # Основной файл приложения
├── 📁 func/                       # Модули функциональности
│   ├── configXray.py
│   ├── parsing.py
│   ├── tun2proxy.py
│   └── copyPast.py
├── 📁 img/                        # Ресурсы интерфейса
│   ├── icon.ico
│   ├── logo.png
│   ├── ico.png
│   └── ref.png
├── 📄 requirements.txt           # Зависимости Python
├── 📄 pyproject.toml             # Конфигурация проекта
├── 📄 README.md                  # Документация
├── 📄 LICENSE                    # Лицензия MIT
├── 📄 .gitignore                 # Исключения Git
├── 📄 .gitattributes             # Настройки Git
├── 📄 UI_IMPROVEMENTS.md         # Документация UI
└── 📄 KODA.md                    # Документация проекта
```

### 🚫 Исключенные файлы
```
📁 Исключено из репозитория
├── 📁 .venv/                     # Виртуальное окружение
├── 📁 xray/                      # Бинарники XRAY Core
├── 📁 tun2proxy/                 # Бинарники tun2proxy
├── 📁 configs/                   # Пользовательские конфигурации
├── 📁 build/                     # Сборки PyInstaller
├── 📁 dist/                      # Дистрибутивы
└── 📄 *.log                      # Логи и временные файлы
```

## Команды для разработки

### Добавление внешних компонентов
```bash
# Скачать XRAY Core
# https://github.com/XTLS/Xray-core/releases
# Разместить в папке xray/

# Скачать tun2proxy
# https://github.com/tun2proxy/tun2proxy/releases  
# Разместить в папке tun2proxy/

# Добавить только конфигурационные файлы
git add xray/README.txt
git add tun2proxy/config.ini.example
```

### Работа с ветками
```bash
# Создать ветку для новой функции
git checkout -b feature/new-ui-theme

# Переключиться на ветку
git checkout main

# Объединить изменения
git merge feature/new-ui-theme

# Удалить ветку
git branch -d feature/new-ui-theme
```

### Откат изменений
```bash
# Откатить последний коммит
git reset --soft HEAD~1

# Откатить конкретный файл
git checkout HEAD -- winLoadXRAY.py

# Откатить все изменения
git checkout -- .
```

## Лучшие практики

### 📝 Коммиты
- Используйте понятные сообщения коммитов
- Группируйте связанные изменения
- Коммитьте часто, но осмысленно

### 🔒 Безопасность
- Никогда не коммитьте API ключи и пароли
- Регулярно проверяйте `git status`
- Используйте `.gitignore` для конфиденциальных данных

### 📦 Релизы
- Создавайте теги для версий: `git tag v0.84-beta`
- Описывайте изменения в релизах
- Поддерживайте актуальный README.md

## Решение проблем

### Файл не игнорируется
```bash
# Проверить правила
git check-ignore -v filename.txt

# Принудительно удалить из индекса
git rm --cached filename.txt
```

### Большие файлы в репозитории
```bash
# Найти большие файлы
git ls-files | xargs du -h | sort -hr | head -10

# Удалить из истории
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch path/to/large-file' \
  --prune-empty --tag-name-filter cat -- --all
```

Эти настройки Git обеспечивают безопасную и эффективную работу с проектом winLoadXRAY!