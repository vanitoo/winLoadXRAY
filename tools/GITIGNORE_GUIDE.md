# Руководство по использованию .gitignore в winLoadXRAY

## Обзор

Файл `.gitignore` настроен для автоматического исключения ненужных файлов из системы контроля версий Git. Это помогает поддерживать чистоту репозитория и избежать случайного коммита конфиденциальных данных.

## Ключевые исключения

### 🔒 Безопасность
- **Пользовательские данные**: `configs/`, `*.json` (кроме системных)
- **Ключи и сертификаты**: `*.key`, `*.pem`, `*.p12`
- **Учетные данные**: `secrets.txt`, `credentials.txt`

### 💻 Разработка
- **Виртуальные окружения**: `.venv/`, `venv/`, `env/`
- **Кэш Python**: `__pycache__/`, `*.pyc`
- **Сборки**: `build/`, `dist/`, `*.egg-info/`

### 🖥️ Системные файлы
- **Windows**: `.DS_Store`, `Thumbs.db`, `desktop.ini`
- **Временные файлы**: `*.log`, `*.tmp`, `temp/`

### 📦 Внешние компоненты
- **XRAY Core**: `xray/` (большие бинарники)
- **tun2proxy**: `tun2proxy/` (большие бинарники)

## Включенные файлы

Несмотря на общие исключения, следующие файлы **всегда включаются**:

```
✅ requirements.txt              # Зависимости проекта
✅ pyproject.toml               # Конфигурация проекта
✅ img/icon.ico                 # Иконки интерфейса
✅ img/logo.png                 # Логотип
✅ README.md                    # Документация
✅ LICENSE                      # Лицензия
✅ ui_enhancement.py            # Модули интерфейса
✅ ui_themes.py                 # Темы и стили
```

## Добавление внешних компонентов

### XRAY Core
1. Скачайте с https://github.com/XTLS/Xray-core/releases
2. Разместите файлы в папке `xray/`
3. Добавьте в git только малые файлы:
   ```bash
   git add xray/README.txt
   git add xray/config.json.example
   ```

### tun2proxy
1. Скачайте с https://github.com/tun2proxy/tun2proxy/releases
2. Разместите файлы в папке `tun2proxy/`
3. Аналогично добавьте только конфигурационные файлы

## Проверка статуса Git

```bash
# Проверить игнорируемые файлы
git status --ignored

# Проверить конкретный файл
git check-ignore xray/xray.exe

# Посмотреть все правила .gitignore
git config core.excludesfile
```

## Локальные настройки

### Для персональных настроек
Создайте файл `.git/info/exclude` для локальных исключений:

```
# Локальные настройки разработчика
my_configs/
personal_notes.txt
debug_settings.json
```

### Для временных файлов
Добавьте в конец `.gitignore`:

```
# Временные файлы разработки
my_debug.log
temp_experiment.py
```

## Решение проблем

### Файл не игнорируется
1. Проверьте правила в `.gitignore`
2. Убедитесь, что файл не был закоммичен ранее
3. Используйте `git rm --cached` для уже добавленных файлов

### Нужно добавить исключенный файл
```bash
# Принудительно добавить файл
git add --force important_file.txt

# Или добавьте отрицательное правило в .gitignore
!important_file.txt
```

## Лучшие практики

1. **Регулярно проверяйте** статус git: `git status`
2. **Не коммитьте** большие бинарники и пользовательские данные
3. **Документируйте** изменения в `.gitignore` комментариями
4. **Тестируйте** новые правила на тестовом репозитории

Этот `.gitignore` настроен специально для проекта winLoadXRAY и обеспечивает безопасность и чистоту репозитория.